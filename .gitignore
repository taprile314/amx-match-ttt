# Artefactos de compilación / build (el .amxx se publica como release asset, no se versiona)
*.asm
*.lst
compiled/
*.amxx.bak*
apps/plugin/plugins/*.amxx
dist/

# Bundles/zips sueltos (artefactos, no se versionan; el bundle de release va en dist/)
*.zip

# Backups
*.bak
*.bak-*
backups/

# Basura del SO / editores
Thumbs.db
desktop.ini
.DS_Store
*~
*.swp
.vscode/
.idea/

# Logs
*.log

# Credenciales reales (solo se versiona el .example)
apps/plugin/configs/users.ini
apps/rcon-panel/config.json

# Etiquetas de PC del panel (IP->nombre, especifico de cada lab)
apps/rcon-panel/pc-labels.json

# Contador de stats del panel (acumulado del torneo, datos de runtime)
apps/rcon-panel/stats.json

# Equipos + fixture + tabla del torneo (datos de runtime, especifico de cada torneo)
apps/rcon-panel/tournament.json

# Node (por si en el futuro el panel usa dependencias)
node_modules/
